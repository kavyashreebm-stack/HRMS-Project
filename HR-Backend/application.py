from app.main import app as application

# This file acts as the entry point for AWS Elastic Beanstalk
# It imports the FastAPI app instance from app/main.py and renames it to 'application'
# which is the default name AWS looks for.
